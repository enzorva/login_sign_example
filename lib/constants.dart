import 'package:flutter/material.dart';

const kTitleSpace = SizedBox(height: 15);

const kFieldSpace = SizedBox(height: 30);

const title = Row(
  mainAxisAlignment: MainAxisAlignment.start,
  children: [
    Text(
      'Email',
      style: TextStyle(fontWeight: FontWeight.bold),
    ),
  ],
);
